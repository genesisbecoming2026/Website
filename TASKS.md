# Genesis Becoming Website — Outstanding Tasks

## Needs content from Ty before publishing
- [x] Real client testimonial quotes — TNT (Toby & Trina Hathaway), Grow (Kari Bulman), Slow Bloom (Emma Steel, Founder) all gathered and live in `config.js`.
- [x] Founder portrait photo — real photo of Ty. Rebuilt as a split layout (photo + GlassPanel text side by side) instead of full-bleed overlay, since the overlay risked the bio text covering his face depending on viewport height.
- [ ] Review/personalize the draft Founder Intro copy — written in the established voice, but no specific biographical facts (years of experience, etc.) were invented, so it may need real detail added.
- [ ] Confirm genesisbecoming.com is still the correct production domain (used in sitemap.xml, robots.txt, canonical URLs, structured data, favicon/OG meta).
- [ ] **New:** look at `favicon-16x16.png` and `favicon-32x32.png` yourself in a real browser tab. They're generated by centering the full wordmark logo on a square — wordmarks often lose legibility at that size. If it reads as a blurry smudge rather than a recognizable mark, Genesis Becoming would benefit from a dedicated small icon-only mark, separate from the full logo.
- [x] TNT hero image was a corrupted/broken file (rendered as an abstract silhouette) — repointed to a real jobsite photo. Also added a crew photo (Brand Overview) and a branded-gear flatlay (4th Applications card) from Ty's leftover-photos batch.
- [x] Grow hero image was a flattened mockup export with baked-in UI artifacts — replaced with a clean corridor photo + a real coded nameplate (GlassPanel + live text) instead of flattened image text.
- [ ] Slow Bloom has 3 more candidate photos ready (pour-over, menu+latte, bag lineup) from the same leftover-photos batch, not yet wired in — one candidate (a 4th) was a broken/black file. Confirm before wiring in.
- [ ] A "Grow wall-logo" mockup photo was sent but not yet placed anywhere — needs a decision on where it goes, if at all, given the corridor photo already replaced the hero.

## Needs a decision from Ty
- [ ] Structured data currently uses `ProfessionalService` schema, not `LocalBusiness` — confirm this is accurate (LocalBusiness implies customers visit a physical location).
- [ ] No Privacy Policy or Terms of Service page exists anywhere on the site. Site doesn't currently collect data directly (Contact page only links to Calendly + mailto), so urgency is low — but worth adding before any future analytics or contact form.
- [ ] Homepage is now 9 major sections long (Hero → Diagnosis → Identity System → Solution → Proof → Testimonials → Services → Founder → Invitation). Watch scroll-depth analytics once live; consider moving Founder to the Our Approach page if drop-off increases.

## Needs verification once live — run SMOKE-TEST-CHECKLIST.md after every deploy from now on
- [ ] Real URL routing — does clicking each nav link actually change the browser URL, and does refresh/back-button work correctly?
- [ ] Grow's black-screen crash fix (`fontFamily` reference bug) — confirm it's actually resolved.
- [ ] All three project pages showing real photos, not gradient placeholders.
- [ ] Custom cursors per project (TNT crosshair, Grow soft ring, Slow Bloom blurred dot) — first real cross-browser check.
- [ ] TNT's composed thumbnail image renders correctly on Home and Work cards.
- [ ] Visible breadcrumbs (Home / Work / Project) display correctly on all three project pages.
- [ ] Mobile nav tap targets (recently resized) actually feel right on a real phone.
- [ ] **New:** the reworked hero — floating glass logo mark in the empty right-side space, refined GlassPanel realism (specular highlight, layered shadow) — actually looks right, not just balances in code.
- [ ] **New:** Identity System's denser, organic-branching node layout and flowing light current — confirm it reads as intended and doesn't feel cluttered with the added micro-particles.
- [ ] **New:** Studio's process steps actually expand/collapse correctly on click.
- [ ] **New:** favicon/OG image — check the browser tab icon, and paste the site URL into something like Slack or Twitter/X to confirm the social preview card looks right.

## Known deliberate tradeoffs (not bugs — decisions made and reasoned through)
- Nav CTA button keeps the shorter "Book a Discovery Call" label instead of matching the longer version used elsewhere, due to nav space constraints.
- "Start Your Project" is an intentionally different CTA label (Home/Work pages) from "Book a Complimentary Discovery Call" used elsewhere — contextual variation, not an inconsistency to fix.

## Reference — where things stand
- Full positioning/messaging rewrite: done (hero, Identity System, Studio audience section, Philosophy → Our Approach).
- Real brand assets wired in for all three case studies: done.
- SEO: per-page meta/OG/Twitter tags, canonical URLs, Organization/Person/Service/Breadcrumb structured data (+ visible breadcrumb UI), sitemap, robots.txt, lazy-loading, heading hierarchy (h1 fix), real favicon set + PWA manifest + dedicated OG image — done.
- Micro-interactions (button squish, heading hover, card accent bar, TNT logo hover, project cursors) — done.
- Accessibility: touch targets, contrast verified, reduced-motion respected site-wide — done.
- Trust-building sections: Testimonials (3 real quotes live), Founder Intro (draft copy, real photo, split layout), Studio progressive disclosure — done.
- Design consistency pass: fixed hardcoded colors that duplicated existing tokens — done.
- Homepage opening (hero → diagnosis → identity network) merged into one continuous scroll sequence (`OpeningSequence.jsx`), replacing two separate stacked components — done.
- Case study depth: all three now have real Strategy sections, merged in from Ty's v46 export (a parallel Claude.ai conversation). TNT: the actual Toby & Trina origin story. Slow Bloom: the mood-board-driven stained-glass concept and the founders' anti-fast-coffee philosophy. Grow: the "almost didn't happen" fingerprint pivot, the collaborative addition of the leaf/G with Kari, rejected alternatives (tree rings, shoreline), and the name's scriptural origin (Colossians). All three case studies now show real strategic thinking, not just problem-to-finished-logo.
